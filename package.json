{
  "name": "520-project-israel-kayci",
  "version": "1.0.0",
  "description": "Express + React app",
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "build": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "author": "Kayci Davila & Israel Aristide",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "supertest": "^6.3.3"
  }
}
